[package]
name = "zed_hvp"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
path = "src/lib.rs"
crate-type = ["cdylib"]

[dependencies]
# 0.7.0 is the current default/max version on crates.io as of 2026-08-12, compatible
# with Zed 0.192.x per crates/extension_api/README.md's compatibility table.
zed_extension_api = "0.7.0"
